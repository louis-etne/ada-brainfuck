package Internals.IO is

   procedure Read (Filename : String);
   -- Read the given filename and execute it
   
   procedure Run (Symbols : String);
   -- Execute the given symbols

   procedure Display (Data : String);
   -- Display the given string
   
end Internals.IO;
